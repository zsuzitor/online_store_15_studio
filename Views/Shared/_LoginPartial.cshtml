@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
    {
    @Html.AntiForgeryToken()
<div>
    <div class="Main_header_1_link_for_4rlink_r div_inline_block">
    <!-- Html.ActionLink(User.Identity.GetUserName(), "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })-->  
        @Html.ActionLink("Manage", "Index", "Manage", routeValues: null, htmlAttributes: new {  })
    </div>
    <div class="Main_header_1_link_for_4rlink_r div_inline_block"><a href="javascript:document.getElementById('logoutForm').submit()">Выйти</a></div>
</div>
    
    }
}
else
{
    <div>
        <div class="Main_header_1_link_for_4rlink_r div_inline_block">
            @Html.ActionLink("Регистрация", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })
        </div>
        <div class="Main_header_1_link_for_4rlink_r div_inline_block">
            @Html.ActionLink("Выполнить вход", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })
        </div>
    </div>
    
}
